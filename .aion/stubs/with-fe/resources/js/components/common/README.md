# Common Components
This directory houses reusable components that, while not acting as core UI primitives, are still shared frequently across different domains or pages. Examples include application-specific navigation bars, shared complex widgets, or standard table layouts.
