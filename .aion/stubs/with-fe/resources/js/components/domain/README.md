# Domain Components
This directory is for domain-specific components that contain business logic or are tightly coupled to a specific feature area of the application. These components are typically constructed by composing smaller UI primitives together.
