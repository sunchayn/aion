# UI Components
This directory contains foundational, highly reusable, and style-driven UI primitives (like buttons, modals, and inputs). These components are intentionally agnostic of business logic and form the basic building blocks for the rest of the application.
