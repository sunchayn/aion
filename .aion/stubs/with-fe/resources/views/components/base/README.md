# Base Components
This directory contains foundational, style-driven Blade UI primitives (like buttons, cards, and form inputs). These components are intentionally agnostic of business logic and form the basic building blocks for the rest of the application's views.
