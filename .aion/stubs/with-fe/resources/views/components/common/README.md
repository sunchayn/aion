# Common Components
This directory houses reusable Blade components that are shared frequently across different domains or pages, but are more complex than basic UI primitives. Examples include standard table layouts, navigation bars, or application-specific shared widgets.
